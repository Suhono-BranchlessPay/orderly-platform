# Example secrets for tenant Kirin (Henderson, KY)
# Copy these into /var/www/samurai-resto/ecosystem.config.cjs → apps[0].env
# DO NOT commit real values. Samurai keeps using unprefixed SQUARE_* / DOORDASH_*.
#
# Square (Kirin location — from Square Developer Dashboard, production)
# TENANT_KIRIN_SQUARE_ACCESS_TOKEN: "EAAAxxxx",
# TENANT_KIRIN_SQUARE_LOCATION_ID: "Lxxxxxxxx",
# TENANT_KIRIN_SQUARE_APPLICATION_ID: "sq0idp-xxxx",
# TENANT_KIRIN_SQUARE_ENVIRONMENT: "production",
#
# DoorDash Drive (Kirin business — or same DD developer app with Kirin store)
# TENANT_KIRIN_DOORDASH_DEVELOPER_ID: "xxxx",
# TENANT_KIRIN_DOORDASH_KEY_ID: "xxxx",
# TENANT_KIRIN_DOORDASH_SIGNING_SECRET: "xxxx",
#
# BranchlessPay post-pay anchor (optional per tenant)
# TENANT_KIRIN_BRANCHLESSPAY_LICENSE_KEY: "bp_live_xxxx",
#
# After editing ecosystem.config.cjs:
#   pm2 delete samurai-api && pm2 start ecosystem.config.cjs && pm2 save
#
# Menu: do NOT seed from Samurai. After Square is connected, import Kirin's
# own catalog (SKUs must match Kirin Square item variations).
#
# Verify:
#   curl -s -H "Host: kirinhibachiexpress.com" http://localhost:8080/api/square/config
#   curl -s -H "Host: kirinhibachiexpress.com" http://localhost:8080/api/owner/integrations?pin=YOUR_PIN
#   curl -s -H "Host: kirinhibachiexpress.com" http://localhost:8080/api/menu/items
#   # expect [] until Kirin catalog is imported
