# Example secrets for tenant Kirin (Henderson, KY)
# Copy into ecosystem.config.cjs → apps[0].env — DO NOT commit real values.
#
# MONEY PATH IS FAIL-CLOSED:
#   TENANT_{SLUG}_SQUARE_* is required (or Square OAuth row for that tenant).
#   Bare SQUARE_* is NEVER used as a fallback (that routed Kirin → Samurai).
#
# Square — Kirin location LRKJ8G89JNNTR / merchant MLRNMQAJ7ERYC
# Prefer OAuth with scopes:
#   ITEMS_READ ITEMS_WRITE ORDERS_READ ORDERS_WRITE
#   PAYMENTS_READ PAYMENTS_WRITE MERCHANT_PROFILE_READ REPORTING_READ
# Or set env tokens:
# TENANT_KIRIN_SQUARE_ACCESS_TOKEN: "EAAAxxxx",
# TENANT_KIRIN_SQUARE_LOCATION_ID: "LRKJ8G89JNNTR",
# TENANT_KIRIN_SQUARE_APPLICATION_ID: "sq0idp-xxxx",
# TENANT_KIRIN_SQUARE_ENVIRONMENT: "production",
#
# Samurai must ALSO use prefixed keys (mirror former globals):
# TENANT_SAMURAI_SQUARE_ACCESS_TOKEN: "<same as old SQUARE_ACCESS_TOKEN>",
# TENANT_SAMURAI_SQUARE_LOCATION_ID: "<same as old SQUARE_LOCATION_ID>",
# TENANT_SAMURAI_SQUARE_APPLICATION_ID: "<same as old SQUARE_APPLICATION_ID>",
# TENANT_SAMURAI_SQUARE_ENVIRONMENT: "production",
#
# Tax: set in DB (not env) — scripts/migrate-tenant-tax-rate.sql
#   Samurai Martinsville IN → 0.07
#   Kirin Henderson KY → 0.06 (confirmed)
#   Samurai Linton → NULL until Greene County rate confirmed
#
# Verify isolation:
#   curl -s -H "Host: kirinhibachiexpress.com" http://127.0.0.1:8080/api/square/config
#   # expect enabled:false until Kirin creds exist — NOT Samurai locationId
#   curl -s -H "Host: samurairesto.com" http://127.0.0.1:8080/api/square/config
#   # expect Samurai location only when TENANT_SAMURAI_SQUARE_* set
#
# Deploy: bash scripts/deploy-samurai-main.sh
# Checklist: docs/TENANT_ONBOARDING_CHECKLIST.md
